{
  "name": "zomatify",
  "version": "1.0.0",
  "description": "A modern food delivery platform with vendor management, real-time order tracking, and integrated payment processing",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "frontend:dev": "cd frontend && npm start",
    "backend:dev": "cd backend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "backend:build": "cd backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build:all": "npm run frontend:build && npm run backend:build",
    "test": "cd frontend && npm test && cd ../backend && npm test",
    "heroku-postbuild": "npm run install:all && npm run build:all"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Aryaman129/Zomatify.git"
  },
  "keywords": [
    "food-delivery",
    "react",
    "nodejs",
    "typescript",
    "supabase",
    "razorpay",
    "real-time",
    "vendor-management"
  ],
  "author": "Aryaman129",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
